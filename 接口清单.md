# 项目接口清单

## 1. 认证模块 (auth.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 登录 | /auth/login | POST | 用户登录 |
| 退出登录 | /auth/logout | POST | 用户退出 |
| 注册 | /auth/register | POST | 用户注册 |
| 获取当前用户菜单 | /auth/getMenus | POST | 获取当前登录用户的菜单数据 |

## 2. 用户管理模块 (api.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 上传头像 | /toupload | POST | 上传用户头像 |
| 获取用户列表 | /users/all | POST | 获取所有用户列表 |
| 获取当前用户信息 | /api/users/me | GET | 获取当前登录用户信息 |
| 编辑用户 | /users/setUser | POST | 更新用户信息 |
| 新建用户 | /users/addUser | POST | 创建新用户 |
| 重置用户密码 | /users/resetPassword | POST | 重置用户密码 |
| 删除用户 | /users/deleteUser | POST | 删除用户 |
| 获取角色词典 | /getRoleDict | GET | 获取角色下拉选项 |
| 更新用户头像 | /users/updateAvatar | POST | 更新用户头像 |

## 3. 菜单管理模块 (menus.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 获取菜单列表 | /getMenus | GET | 获取所有菜单 |
| 获取菜单树 | /getMenuTree | GET | 获取菜单树形结构 |
| 设置菜单 | /setMenu | POST | 更新菜单信息 |
| 删除菜单 | /deleteMenu | POST | 删除菜单 |
| 新增菜单 | /addMenu | POST | 创建新菜单 |

## 4. 角色管理模块 (role.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 获取所有角色 | /getRoles | POST | 获取角色列表 |
| 设置角色 | /setRole | POST | 更新角色信息 |
| 新增角色 | /addRole | POST | 创建新角色 |
| 删除角色 | /delRole | POST | 删除角色 |

## 5. 文件管理模块 (upload.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 上传单个文件 | /file/upload | POST | 上传文件 |
| 上传单张图片 | /files/upload-image | POST | 上传图片 |
| 上传整个文件夹 | /files/upload-folder | POST | 上传文件夹 |
| 文件分片上传 | /fragmentUpload | POST | 分片上传大文件 |
| 文件分片下载 | /fragmentDownload | POST | 分片下载大文件 |

## 6. 组织架构模块 (api.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 获取组织架构树 | /org-structures/tree | POST | 获取组织树形结构 |
| 获取组织详情 | /org-structures/detail | GET | 获取组织详细信息 |
| 创建组织 | /org-structures/create | POST | 创建新组织 |
| 更新组织 | /org-structures/update | POST | 更新组织信息 |
| 删除组织 | /org-structures/delete | POST | 删除组织 |

## 7. 工作台模块 (workbenches.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| 获取工作台管理列表 | /api/workbenches/list | GET | 获取所有工作台 |
| 获取当前用户工作台 | /api/workbenches/current/list | GET | 获取当前用户所属工作台 |
| 新增工作台子应用 | /api/workbenches/create | POST | 创建工作台 |
| 编辑工作台子应用 | /api/workbenches/update/{id} | PUT | 更新工作台 |
| 删除工作台子应用 | /api/workbenches/delete/{id} | DELETE | 删除工作台 |

## 8. AI聊天模块 (chat.ts)

| 接口名称 | 接口地址 | 请求方式 | 说明 |
|---------|---------|---------|------|
| DeepSeek聊天 | /deepseek/chat | POST | AI对话接口 |

---

## 接口统计

- **总接口数量**: 32个
- **GET请求**: 9个
- **POST请求**: 20个
- **PUT请求**: 1个
- **DELETE请求**: 2个

## 基础配置

- **基础路径**: `/api`
- **超时时间**: 300秒
- **认证方式**: Bearer Token
- **请求头**: application/json;charset=UTF-8